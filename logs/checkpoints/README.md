# Checkpoints

Model checkpoints are saved here during training.

- `best_ep{N}_f1{score}.pt` — best checkpoint by macro F1
- `latest.pt` — most recent epoch checkpoint
- `BEST_OVERALL_{model}_f1{score}.pt` — best across all agent runs
